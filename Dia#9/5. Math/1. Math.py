"""
Módulo que contiene todo lo importante para operar con números
desde lo básico hasta trigonometría y más.
"""

import math

floor = math.floor(2.31) # Fucnión piso
ceil = math.ceil(32.1) # Función techo
pi = math.pi # Número pi
log = math.log(100, 32) # Función logaritmica (resultado esperado, número a elevar)
cos = math.cos(100) # Función coseno
sin = math.sin(100) # Función seno